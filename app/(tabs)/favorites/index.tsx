import { View, Text } from 'react-native';

export default function FavoritesScreen() {
	return (
		<View className="bg-black w-full h-screen">
			<Text className="text-white text-base font-geist">FavoritesScreen</Text>
		</View>
	);
}
